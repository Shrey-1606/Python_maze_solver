# Python_maze_solver
This is a basic level python based maze solver, it has a certain format of maze_map.txt it works with it. Only works for maze having unique solution and also gives a visual output of how the maze is solved.
